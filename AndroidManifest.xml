<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="br.com.efinancas.android"
          android:versionCode="1"
          android:versionName="1.0">

    <uses-sdk android:minSdkVersion="10"/>
    <application android:label="@string/app_name"
            android:icon="@drawable/efinancas">
        <activity
                android:theme="@android:style/Theme.Holo"
                android:logo="@drawable/efinancas"
                android:name="MyActivity"
                android:label="EFinanças">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <activity
            android:theme="@android:style/Theme.Holo"
            android:logo="@drawable/ic_transacao"
            android:name=".EditarTransacao"
            android:label="Editar Transação">
        </activity>
        <activity
                android:theme="@android:style/Theme.Holo"
                android:logo="@drawable/efinancas"
            android:name=".EditarConta"
            android:label="Editar Conta">
        </activity>
        <activity
                android:theme="@android:style/Theme.Holo"
                android:logo="@drawable/ic_transacao"
            android:name=".ListarTransacoesPorConta"
            android:label="Detalhes das Transações">
        </activity>
    </application>
</manifest> 
